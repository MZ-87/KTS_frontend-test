function patchArrays(...arg) {

    try {
        if (typeof arg !== 'string') {
            throw new Error('INVALID_ARGUMENT');
        }

        return sortedStr;
    } catch (e) {
        return e;
    }
}

